<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/head %>
  </head>
  <body>
    
    <% include ./partials/nav %>

    <div class="container">
      <h2><%= reqUser.username %> library</h2>
      <h4>From <%= reqUser.country %>, <%= reqUser.city %>.</h4>
      <% for(var i=books.length-1; i >= 0; i--) { %>
            <div class="thumbnail">
              <img class="thumbnail_image" src="<%= books[i].imageLink %>" alt="<%= books[i].title %>" >
              <div class="user">
                <p> <%= books[i].author %> <br>
                <%= books[i].title %> </p>
              </div>
            </div>
        <% } %>

    </div><!-- /.container -->

  </body>
</html>